#include<stdio.h>
int main(){
    int a=1, b=1;
    while (a <=100)
    {
        if (a % 2==1)
        {
         printf("%d\n",a);   
        }
        
        //printf("A= i will not misbehav in class agin\n");
        a+=1;
    }
    /*
    do
    {
      printf("B= i will not misbehav in class agin\n");
        b+=1;
    } while (b % 2==1);
    
    */
   // for multiple table printf("5 X %d = %d", a , (a*5));
}